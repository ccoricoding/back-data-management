# 백데이터 관리 시스템 (Back Data Management)

대구광역시 소속 도서관들의 백데이터를 관리하는 웹 애플리케이션입니다.

## 🚀 기능

- **사용자 인증**: 로그인/회원가입 및 관리자 승인 시스템
- **대시보드**: 전체 데이터 통계 및 현황 요약
- **데이터 입력**: 도서관별 백데이터 입력 양식
- **현황 조회**: 필터링 및 검색 기능
- **Excel 다운로드**: 현황 데이터를 Excel로 내보내기
- **카테고리 관리**: 각종 카테고리 항목 관리
- **사용자 관리**: 사용자 승인, 권한 관리 (관리자용)

## 🛠 기술 스택

- **Frontend**: React 19.2 + Vite
- **스타일링**: TailwindCSS
- **라우팅**: React Router DOM v7
- **데이터베이스**: Supabase
- **아이콘**: Lucide React
- **날짜 처리**: date-fns
- **Excel**: xlsx, xlsx-js-style, file-saver

## 📋 대상 도서관

- 국채보상운동기념도서관
- 대구2·28기념학생도서관
- 대구광역시립 동부/서부/남부/북부/수성/두류/달성도서관
- 대구광역시교육청 삼국유사군위도서관

## 🔧 개발 환경 설정

### 1. 저장소 클론

```bash
git clone <repository-url>
cd back-data-management
```

### 2. 의존성 설치

```bash
npm install
```

### 3. 환경 변수 설정

`.env` 파일을 프로젝트 루트에 생성하고 다음 내용을 추가하세요:

```env
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

### 4. 개발 서버 실행

```bash
npm run dev
```

로컬 서버가 `http://localhost:5173`에서 실행됩니다.

## 📦 프로덕션 빌드

```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

## 🗄️ Supabase 데이터베이스 구조

### users 테이블
- `id` (uuid, primary key)
- `name` (text, unique)
- `password` (text)
- `library_name` (text)
- `is_admin` (boolean)
- `is_approved` (boolean)
- `created_at` (timestamp)

### categories 테이블
- `id` (uuid, primary key)
- `key` (text, unique)
- `items` (jsonb)
- `updated_at` (timestamp)

### data_entries 테이블
- `id` (uuid, primary key)
- `user_id` (uuid, foreign key → users)
- `overview` (jsonb)
- `budget` (jsonb)
- `performances` (jsonb)
- `created_at` (timestamp)
- `updated_at` (timestamp)

## 🌐 배포

### Vercel 배포 (권장)

1. [Vercel](https://vercel.com) 계정 생성
2. GitHub 저장소 연결
3. 환경 변수 설정:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. 자동 배포 완료!

### 기타 호스팅

- **Netlify**: Drag & Drop으로 간편 배포
- **GitHub Pages**: 정적 사이트 호스팅
- **Railway**: 백엔드가 필요한 경우

## 📱 주요 페이지

- `/login` - 로그인 및 회원가입
- `/` - 대시보드
- `/calendar` - 달력 뷰
- `/input` - 데이터 입력
- `/status` - 현황 조회
- `/categories` - 카테고리 관리
- `/profile` - 사용자 프로필
- `/admin` - 사용자 관리 (관리자만)

## 🔐 보안

- 환경 변수를 통한 API 키 관리
- 사용자 승인 시스템
- 관리자 권한 분리
- Supabase Row Level Security (RLS) 권장

## 📄 라이선스

이 프로젝트는 대구광역시 도서관 내부 사용을 위한 프로젝트입니다.

## 👥 기여

문의사항이나 개선 사항은 관리자에게 연락해주세요.
